{
  "name": "agent_list",
  "description": "List all available agents with their configurations. This allows agents to discover what agents exist and their properties.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "mode": {
        "type": "string",
        "enum": ["subagent", "primary", "all", "worker", "system"],
        "description": "Optional filter by agent mode. If not provided, returns all agents regardless of their mode setting."
      },
      "includeHidden": { "type": "boolean", "default": false, "description": "Include hidden agents in the results" },
      "format": { "type": "string", "enum": ["summary", "detailed"], "default": "summary", "description": "Output format: summary (compact) or detailed (full info)" }
    },
    "required": []
  }
}
