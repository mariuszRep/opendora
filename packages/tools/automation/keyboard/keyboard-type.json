{
  "name": "pyautogui_keyboard_type",
  "description": "Type a string of text into the currently focused window, one character at a time. Use `pyautogui_mouse_click` first to focus the target input field.\n\nFor special keys (Enter, Tab, etc.) use `pyautogui_keyboard_press` instead.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": { "type": "string", "description": "Text to type" }
    },
    "required": ["text"]
  }
}
