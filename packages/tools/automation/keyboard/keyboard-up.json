{
  "name": "pyautogui_keyboard_up",
  "description": "Release a held keyboard key.\n\nPair with keyboard_down to complete manual key combinations. Accepts xdotool key names (e.g. 'shift', 'ctrl', 'alt', 'Return', 'F1').",
  "inputSchema": {
    "type": "object",
    "properties": {
      "key": { "type": "string", "description": "Key name in xdotool format (e.g. 'shift', 'ctrl', 'alt', 'Return')" }
    },
    "required": ["key"]
  }
}
