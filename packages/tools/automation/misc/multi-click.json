{
  "name": "pyautogui_multi_click",
  "description": "Click multiple screen coordinates in sequence. Optionally hold Ctrl between clicks for multi-select (e.g. selecting multiple files).\n\nEach point in `points` must have `x` and `y`; `button` defaults to \"left\".",
  "inputSchema": {
    "type": "object",
    "properties": {
      "points": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "x": { "type": "integer" },
            "y": { "type": "integer" },
            "button": { "type": "string", "enum": ["left", "right", "middle"] }
          },
          "required": ["x", "y"]
        },
        "description": "List of coordinates to click"
      },
      "hold_ctrl": { "type": "boolean", "description": "Hold Ctrl key during all clicks (for multi-select)" },
      "delay_ms": { "type": "integer", "description": "Milliseconds between clicks (default 100)" }
    },
    "required": ["points"]
  }
}
