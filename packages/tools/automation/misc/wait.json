{
  "name": "pyautogui_wait",
  "description": "Pause for a given number of seconds before the next action. Use between actions that need time for animations or app state changes to settle.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "duration": { "type": "number", "description": "Seconds to wait (e.g. 0.5, 1, 2)" }
    },
    "required": ["duration"]
  }
}
