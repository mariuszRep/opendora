{
  "name": "pyautogui_screen_locate_all",
  "description": "Find all occurrences of a needle image on the screen using template matching.\n\nReturns center coordinates for each match. Use a lower confidence for fuzzy matching. Nearby duplicate matches are automatically deduplicated.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "image": { "type": "string", "description": "Absolute path to the needle image file to search for" },
      "confidence": { "type": "number", "minimum": 0, "maximum": 1, "description": "Match threshold 0.0–1.0 (default: 0.9)" }
    },
    "required": ["image"]
  }
}
