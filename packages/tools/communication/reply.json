{
  "name": "reply",
  "description": "Post a message silently into another session. This does NOT trigger the LLM there.\nThe message appears in that session and is VISIBLE to the user or agent there.\n\nreply is the mechanism that fulfills the return-path contract created by reply_to.\nWhen a delegating agent sets reply_to, they established a binding contract declaring\nwhere results and status must surface. reply is how you honor that contract.\n\n─────────────────────────────────────────\nWHAT reply IS FOR\n─────────────────────────────────────────\n\nreply is for reporting back upstream — not for interactive dialogue.\n\nUse reply when you have a result, summary, status update, or partial finding to\nsurface into the upstream session. It posts a silent message there and does not\nblock or trigger the recipient's LLM.\n\nUse reply for:\n- final results and requirement summaries\n- status updates and progress reports\n- partial findings worth surfacing\n- brief status when blocking or handing back\n\nDo NOT use reply to ask the user questions. If you need human input, use question.\nreply has no return path for the user's answer — it is a one-way post.\n\n─────────────────────────────────────────\nHARD RULE — BINDING CONTRACT\n─────────────────────────────────────────\n\nIf your session context shows \"Reply expected: silent\" and \"Reply to session ID: ses_xxx\",\na return-path contract exists. When your work is complete (or you have status to report),\nyou MUST use reply to surface that output upstream.\n\nThis is unconditional. There are no exceptions. No workarounds.\n\n─────────────────────────────────────────\nWHAT reply DOES\n─────────────────────────────────────────\n\n- Posts a USER-VISIBLE message in the upstream session (the reply_to target)\n- Includes navigation back to YOUR session so the requester can find you\n- Does NOT trigger the target agent's LLM — the message just appears\n- Surfaces your delegated session into the visible conversation\n\n─────────────────────────────────────────\nMUST NOT\n─────────────────────────────────────────\n\n- Do NOT use reply to ask the user for input — use question instead.\n- Do NOT keep results or status only inside your delegated session.\n- Do NOT assume the requester can see your session without a reply surfacing it.\n\n─────────────────────────────────────────\nHOW ROUTING WORKS\n─────────────────────────────────────────\n\nThe system resolves the reply target automatically from your session context:\n1. If replyToSessionID is set on your session — reply goes there (set by delegate tool)\n2. Otherwise — reply goes to your parentSessionID (whoever delegated to you)\n3. If neither exists — error (orphaned session)\n\nYou do not need to specify a session. Just call reply with your message.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string",
        "description": "Text content to post into the target session."
      }
    },
    "required": ["message"]
  }
}
