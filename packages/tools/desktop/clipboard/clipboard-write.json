{
  "name": "desktop_clipboard_write",
  "description": "Write text to the system clipboard, replacing its current content.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "text": { "type": "string", "description": "Text to place in the clipboard" }
    },
    "required": ["text"]
  }
}
