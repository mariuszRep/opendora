{
  "name": "desktop_keyboard_press",
  "description": "Press a key or key combination such as \"ctrl+c\", \"enter\", or \"ctrl+shift+t\".\n\nSupported modifiers: ctrl, shift, alt, meta / cmd / win / super.\nSpecial keys: enter, esc, space, tab, backspace, delete, insert,\n  up, down, left, right, home, end, pageup, pagedown,\n  capslock, numlock, printscreen, pause.\nLetters a-z, digits 0-9, and function keys f1-f24 are also supported.\n\nAll parts of a combo are pressed simultaneously, then released together.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "keys": { "type": "string", "description": "Key or combination, e.g. 'ctrl+c', 'enter', 'ctrl+shift+t'" }
    },
    "required": ["keys"]
  }
}
