{
  "name": "desktop_mouse_click",
  "description": "Click a mouse button at the current cursor position, or at optional (x, y) coordinates.\n\nIf `x` and `y` are provided the cursor moves there first, then clicks.\n`button` defaults to \"left\". Set `double: true` for a double-click.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "button": { "type": "string", "enum": ["left", "right", "middle"], "description": "Mouse button to click (default: left)" },
      "double": { "type": "boolean", "description": "Double-click instead of single-click" },
      "x": { "type": "number", "description": "X coordinate to move to before clicking" },
      "y": { "type": "number", "description": "Y coordinate to move to before clicking" }
    },
    "required": []
  }
}
