{
  "name": "desktop_mouse_scroll",
  "description": "Scroll the mouse wheel in a direction by a given number of steps.\n\n`direction` must be one of: \"up\", \"down\", \"left\", \"right\".\n`amount` is the number of scroll steps (positive integer).",
  "inputSchema": {
    "type": "object",
    "properties": {
      "direction": { "type": "string", "enum": ["up", "down", "left", "right"], "description": "Scroll direction" },
      "amount": { "type": "number", "description": "Number of scroll steps" }
    },
    "required": ["direction", "amount"]
  }
}
