{
  "name": "pyautogui_keyboard_down",
  "description": "Hold a keyboard key down without releasing it.\n\nUse with keyboard_up to perform key combinations manually (e.g. hold Shift while clicking). Accepts xdotool key names (e.g. 'shift', 'ctrl', 'alt', 'Return', 'F1').",
  "inputSchema": {
    "type": "object",
    "properties": {
      "key": { "type": "string", "description": "Key name in xdotool format (e.g. 'shift', 'ctrl', 'alt', 'Return')" }
    },
    "required": ["key"]
  }
}
