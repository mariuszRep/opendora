{
  "name": "pyautogui_screen_pixel",
  "description": "Read the RGB color of a single pixel at screen coordinate (x, y).\n\nTakes a screenshot internally to sample the color. Returns r, g, b values and a hex string.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "x": { "type": "integer", "description": "X screen coordinate" },
      "y": { "type": "integer", "description": "Y screen coordinate" }
    },
    "required": ["x", "y"]
  }
}
