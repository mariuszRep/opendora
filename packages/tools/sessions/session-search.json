{
  "name": "session_search",
  "description": "Search and fetch sessions in OpenDora. This tool allows agents to find existing sessions and retrieve their details, enabling session switching and management.\n\nUse this tool to:\n- Find sessions by title, type, status, or agent\n- Get specific session details by ID\n- List root sessions or filter by directory\n- Search for sessions to switch context\n- Find only sessions owned by the current agent (owned_only: true)\n\nTitle search supports glob patterns (similar to the Glob tool):\n- Plain term: \"auth\" — case-insensitive substring match\n- Wildcard: \"*login*\" — matches any title containing \"login\"\n- Prefix: \"feature*\" — matches titles starting with \"feature\"\n- Single char: \"fix-issue-?\" — matches with any single-character wildcard\n\nOwnership filtering:\n- owned_only: true — restricts results to sessions belonging to the calling agent (resolved by agent storage ID, not display name)\n- owned_only: false (default) — returns sessions across all agents\n- agent_id — filter by a specific agent ID (mutually exclusive with owned_only)\n\nSession markers in results:\n- is_current_session: true — this session is the one this tool was called from (self-reference)\n- is_owned_by_searcher: true — this session belongs to the calling agent (present on all matching results, not only when owned_only is set)\n\nExclusion:\n- exclude_session_id — a single session ID or array of IDs to exclude from results; useful for removing the current session from candidate lists without suppressing is_current_session on other lookups\n\nThe tool supports various filters and can return detailed session information including metadata, message counts, and timestamps.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Search query to filter sessions by title (case-insensitive). Supports glob patterns: '*keyword*' matches titles containing keyword, 'prefix*' matches titles starting with prefix, 'a?c' uses single-character wildcard. Plain terms use substring matching."
      },
      "directory": {
        "type": "string",
        "description": "Filter sessions by project directory"
      },
      "session_type": {
        "type": "string",
        "enum": ["role", "scope", "worker", "scratchpad"],
        "description": "Filter by session type"
      },
      "status": {
        "type": "string",
        "enum": ["active", "archived", "closed"],
        "description": "Filter by session status"
      },
      "agent_id": {
        "type": "string",
        "description": "Filter by assigned agent ID"
      },
      "owned_only": {
        "type": "boolean",
        "default": false,
        "description": "When true, only return sessions owned by the calling agent (resolved by agent ID, not display name). When false (default), sessions across all agents are returned. Cannot be combined with agent_id."
      },
      "exclude_session_id": {
        "oneOf": [
          { "type": "string" },
          { "type": "array", "items": { "type": "string" } }
        ],
        "description": "Session ID or list of session IDs to exclude from results. Useful for removing the current session from candidate lists."
      },
      "roots_only": {
        "type": "boolean",
        "default": false,
        "description": "Only return root sessions (no parent)"
      },
      "limit": {
        "type": "number",
        "description": "Maximum number of sessions to return"
      },
      "start_after": {
        "type": "number",
        "description": "Filter sessions updated after this timestamp (ms since epoch)"
      },
      "session_id": {
        "type": "string",
        "description": "Get specific session by ID (overrides other filters)"
      }
    },
    "required": []
  }
}
