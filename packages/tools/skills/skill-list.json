{
  "name": "skill_list",
  "description": "List available skills. Without arguments: lists ALL skills on the system (unrestricted, for discovery and agent management). With agent='<name>': lists only the skills assigned to that agent, useful before delegating a task.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "agent": {
        "type": "string",
        "description": "Agent name or ID. When provided, returns only the skills assigned to that agent."
      }
    },
    "required": []
  }
}
