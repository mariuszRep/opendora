{
  "name": "skill_remove",
  "description": "Remove an installed or locally created skill by name. Deletes the skill directory and reloads the skill registry. This is irreversible — the skill files will be deleted.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Name of the skill to remove"
      }
    },
    "required": ["name"]
  }
}
