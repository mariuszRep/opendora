{
  "name": "workflow_delete",
  "description": "Permanently delete a workflow definition by ID. This cannot be undone. Use workflow_list to confirm the ID before deleting.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "ID of the workflow to delete"
      }
    },
    "required": ["id"]
  }
}
